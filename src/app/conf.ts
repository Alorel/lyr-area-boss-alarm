export const enum StaticConf {
  GAME_TIMEZONE = 'Europe/London',
  KEY_USE_LOCAL_TIME = 'use_local_time',
  KEY_NOTIFY_BEFORE = 'notify_before_boss',
  KEY_NOTIFY_ON_START = 'notify_on_boss_start',
  KEY_SELECTED_TIMES = 'selected_times'
}

export const INITIAL_TITLE = document.title;
